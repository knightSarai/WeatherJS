import template from './forcast.template.js';

const ForcastComponent = {
    bindings: {},
    template,
    controller: class ForcastController {
        constructor() {

        }
    }
}

export default ForcastComponent;